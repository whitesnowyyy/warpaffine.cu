// #include <stdio.h>
// #include <cuda_runtime.h>

// // compute最终会在gpu上，以3个线程启动进行执行
// __global__ void warpAffine_kernel(unsigned char* src, unsigned char* dst, float* M){

//     int position = (blockDim.x * blockIdx.x + threadIdx.x);



// }